export const makeSelectIsGameStarted = (state) => state.pictureSlice.start;

export const makeSelectGameFinish = (state)=> state.pictureSlice.finish;

export const makeSelectJsonOptions = (state) => state.pictureSlice.jsonOptions;

export const makeSelectImage = (state) => state.pictureSlice.img;

export const makeSelectNotFoundItems = (state) => state.pictureSlice.notFoundItems;

export const makeSelectChoosenImageType = (state) => state.pictureSlice.choosenImgType;


